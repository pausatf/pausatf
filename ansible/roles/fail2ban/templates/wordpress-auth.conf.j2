# Fail2ban filter for WordPress authentication failures
# Managed by Ansible
# Requires wp-fail2ban MU plugin (writes to /var/log/auth.log via syslog)

[Definition]
failregex = Authentication failure for .* from <HOST>$
            XML-RPC authentication failure from <HOST>$
            Blocked authentication attempt for .* from <HOST>$
ignoreregex =
