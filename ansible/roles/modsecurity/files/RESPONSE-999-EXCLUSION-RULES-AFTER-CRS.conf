# OWASP CRS — Local post-CRS exclusions
# Add SecRuleRemoveById, SecRuleUpdateActionById, etc. here.
# These are evaluated AFTER the CRS rules load.
#
# Example: unconditionally disable a rule
# SecRuleRemoveById 942100
#
# Example: change blocking action to 404
# SecRuleUpdateActionById 949110 "t:none,deny,status:404"
# SecRuleUpdateActionById 959100 "t:none,deny,status:404"
