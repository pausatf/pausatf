---
- name: Converge
  hosts: all
  become: true
  pre_tasks:
    - name: Install Apache (dependency)
      ansible.builtin.apt:
        name: apache2
        state: present
        update_cache: true
  roles:
    - role: modsecurity
