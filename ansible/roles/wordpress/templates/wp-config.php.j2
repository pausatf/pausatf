<?php
/**
 * WordPress Configuration File
 * Managed by Ansible
 * Environment: {{ wp_site.environment | default('production') }}
 */

// ** MySQL settings ** //
define( 'DB_NAME', '{{ wp_site.db_name | default(wordpress_db_name) | default("wordpress") }}' );
define( 'DB_USER', '{{ wp_site.db_user | default(wordpress_db_user) | default("wpuser") }}' );
define( 'DB_PASSWORD', '{{ wp_site.db_password | default(wordpress_db_password) }}' );
define( 'DB_HOST', '{{ wp_site.db_host | default(wordpress_db_host) | default("localhost") }}' );
define( 'DB_CHARSET', 'utf8mb4' );
define( 'DB_COLLATE', '' );

// ** Authentication Unique Keys and Salts ** //
define('AUTH_KEY',         '{{ wp_site.auth_key | default(wordpress_auth_key) }}');
define('SECURE_AUTH_KEY',  '{{ wp_site.secure_auth_key | default(wordpress_secure_auth_key) }}');
define('LOGGED_IN_KEY',    '{{ wp_site.logged_in_key | default(wordpress_logged_in_key) }}');
define('NONCE_KEY',        '{{ wp_site.nonce_key | default(wordpress_nonce_key) }}');
define('AUTH_SALT',        '{{ wp_site.auth_salt | default(wordpress_auth_salt) }}');
define('SECURE_AUTH_SALT', '{{ wp_site.secure_auth_salt | default(wordpress_secure_auth_salt) }}');
define('LOGGED_IN_SALT',   '{{ wp_site.logged_in_salt | default(wordpress_logged_in_salt) }}');
define('NONCE_SALT',       '{{ wp_site.nonce_salt | default(wordpress_nonce_salt) }}');

// ** WordPress Database Table prefix ** //
$table_prefix = '{{ wp_site.table_prefix | default(wordpress_table_prefix) | default("wp_") }}';

// ** WordPress debugging mode ** //
define( 'WP_DEBUG', {{ 'true' if wp_site.debug_mode | default(false) else 'false' }} );
define( 'WP_DEBUG_LOG', {{ 'true' if wp_site.debug_mode | default(false) else 'false' }} );
define( 'WP_DEBUG_DISPLAY', false );  // Never expose errors on web-accessible sites
define( 'SCRIPT_DEBUG', false );

// ** WordPress Security ** //
define( 'DISALLOW_FILE_EDIT', {{ 'true' if wp_site.disallow_file_edit | default(true) else 'false' }} );
define( 'DISALLOW_FILE_MODS', {{ 'true' if wp_site.disallow_file_mods | default(false) else 'false' }} );
define( 'FORCE_SSL_ADMIN', true );

// ** WordPress Performance ** //
define( 'WP_MEMORY_LIMIT', '{{ wp_site.wp_memory_limit | default(wordpress_memory_limit) | default("256M") }}' );
define( 'WP_MAX_MEMORY_LIMIT', '{{ wp_site.wp_max_memory_limit | default(wordpress_max_memory_limit) | default("512M") }}' );
define( 'WP_CACHE', true );

// ** WordPress Auto-Updates ** //
{% if wp_site.auto_update_core | default('minor') is sameas true %}
define( 'WP_AUTO_UPDATE_CORE', true );
{% elif wp_site.auto_update_core | default('minor') is sameas false %}
define( 'WP_AUTO_UPDATE_CORE', false );
{% else %}
define( 'WP_AUTO_UPDATE_CORE', '{{ wp_site.auto_update_core | default("minor") }}' );
{% endif %}

// ** WordPress Content Directory ** //
define( 'WP_CONTENT_DIR', dirname(__FILE__) . '/wp-content' );
define( 'WP_CONTENT_URL', 'https://{{ wp_site.domain | default(wordpress_site_url) }}/wp-content' );

// ** WordPress Site URLs ** //
define( 'WP_HOME', 'https://{{ wp_site.domain | default(wordpress_site_url) }}' );
define( 'WP_SITEURL', 'https://{{ wp_site.domain | default(wordpress_site_url) }}' );

// ** Increase PHP limits ** //
@ini_set( 'upload_max_filesize' , '100M' );
@ini_set( 'post_max_size', '100M');
@ini_set( 'memory_limit', '256M' );
@ini_set( 'max_execution_time', '300' );
@ini_set( 'max_input_time', '300' );

/* That's all, stop editing! Happy publishing. */

/** Absolute path to the WordPress directory. */
if ( ! defined( 'ABSPATH' ) ) {
    define( 'ABSPATH', __DIR__ . '/' );
}

/** Sets up WordPress vars and included files. */
require_once ABSPATH . 'wp-settings.php';
