---
- name: Configure base system
  hosts: all
  gather_facts: true
  become: true
  roles:
    - role: common
      tags: [common, base]
    - role: tailscale
      tags: [tailscale, networking]
      when: tailscale_enabled | default(false)
