---
- name: Configure monitoring
  hosts: all
  gather_facts: true
  become: true
  roles:
    - role: monitoring
      tags: [monitoring]
    - role: newrelic
      tags: [monitoring, newrelic]
      when: newrelic_enabled | default(false) and newrelic_license_key | default('') | length > 0
