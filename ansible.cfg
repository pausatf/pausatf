[defaults]
# Minimal root-level config for tools (e.g. ansible-lint) that run from the
# git root and don't automatically pick up ansible/ansible.cfg.
# Operational config lives in ansible/ansible.cfg.
roles_path = ansible/roles
