name: Deploy (reusable)

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
        description: "Target environment (production, staging, development)"
      ansible_limit:
        required: true
        type: string
        description: "Ansible --limit value"
      healthcheck_host:
        required: false
        type: string
        default: ""
        description: "SSH host for post-deploy healthcheck (e.g., github-deploy@ftp.pausatf.org)"
    secrets:
      SSH_PRIVATE_KEY:
        required: false
      ANSIBLE_VAULT_PASSWORD:
        required: true
      ANSIBLE_VAULT_FILE:
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v4

      - name: Setup Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v5
        with:
          python-version: '3.12'

      - name: Install Ansible deps
        run: |
          python -m pip install --upgrade pip
          pip install ansible-core==2.17.5 cloudflare requests
          ansible-galaxy collection install -r ansible/requirements.yml

      - name: Setup SSH key
        env:
          SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          if [ -n "$SSH_KEY" ]; then
            install -m 700 -d ~/.ssh
            printf '%s\n' "$SSH_KEY" > ~/.ssh/deploy_key
            chmod 600 ~/.ssh/deploy_key
            ln -sf ~/.ssh/deploy_key ~/.ssh/pausatf-prod
          fi

      - name: Decrypt vault file
        env:
          ANSIBLE_VAULT_FILE: ${{ secrets.ANSIBLE_VAULT_FILE }}
          ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
        run: |
          if [ -z "$ANSIBLE_VAULT_PASSWORD" ] || [ -z "$ANSIBLE_VAULT_FILE" ]; then
            echo "ERROR: ANSIBLE_VAULT_PASSWORD and ANSIBLE_VAULT_FILE secrets are required" >&2
            exit 1
          fi
          printf '%s' "$ANSIBLE_VAULT_FILE" | base64 -d > ansible/group_vars/vault.yml
          echo "$ANSIBLE_VAULT_PASSWORD" > vault.pass
          chmod 600 vault.pass ansible/group_vars/vault.yml

      - name: Run playbook
        working-directory: ansible
        env:
          ANSIBLE_LIMIT: ${{ inputs.ansible_limit }}
        run: |
          RUNNER_IP=$(curl -fsS https://api.ipify.org)
          if [ -z "$RUNNER_IP" ]; then
            echo "ERROR: Could not determine GitHub runner public IP" >&2
            exit 1
          fi

          ansible-playbook -i inventory/hosts.yml site.yml \
            -l "$ANSIBLE_LIMIT" \
            -e "ansible_controller_cidr=${RUNNER_IP}/32" \
            --vault-password-file ../vault.pass

      - name: Healthcheck
        if: ${{ inputs.healthcheck_host != '' }}
        env:
          HC_HOST: ${{ inputs.healthcheck_host }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=accept-new \
            -o IdentitiesOnly=yes \
            "$HC_HOST" \
            "curl -fsS -o /dev/null -w '%{http_code}' http://localhost/ | grep -qE '^(200|301|302)'"

      - name: Cleanup secrets
        if: always()
        run: |
          rm -f vault.pass ansible/group_vars/vault.yml ~/.ssh/deploy_key ~/.ssh/pausatf-prod
